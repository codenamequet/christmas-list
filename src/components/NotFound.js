import React from 'react'

const NotFound = () => (
    <div>
        <h3>It appears that this page doesn't exist! Check the url to make sure you've entered the correct name!</h3>
    </div>
)

export default NotFound
